'use client';

import { useEffect, useState } from 'react';
import Link from 'next/link';
import dynamic from 'next/dynamic';
import Header from '@/components/layout/Header';
import Footer from '@/components/layout/Footer';
import Button from '@/components/ui/Button';
import ChartSkeleton from '@/components/ui/ChartSkeleton';
import LazySection from '@/components/ui/LazySection';
import { useParallax } from '@/lib/hooks/useParallax';

// Динамический импорт тяжелых компонентов для оптимизации загрузки
const SemiDonutChart = dynamic(() => import('@/components/home/SemiDonutChart'), {
  loading: () => <ChartSkeleton />,
  ssr: false,
});

const VerticalBarChart = dynamic(() => import('@/components/home/VerticalBarChart'), {
  loading: () => <ChartSkeleton />,
  ssr: false,
});

const SmoothLineChart = dynamic(() => import('@/components/home/SmoothLineChart'), {
  loading: () => <ChartSkeleton />,
  ssr: false,
});

// FAQ Component with proper spacing
const FAQItem = ({ question, answer }: { question: string; answer: string }) => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <div className="card-glow rounded-xl p-6 hover:border-neon-purple/30 transition-all">
      <button
        type="button"
        onClick={() => setIsOpen(!isOpen)}
        className="w-full flex items-center justify-between text-left gap-4"
        aria-expanded={isOpen ? 'true' : 'false'}
      >
        <h3 className="text-lg font-semibold text-text-primary">{question}</h3>
        <span className={`text-neon-purple transition-transform duration-300 flex-shrink-0 ${isOpen ? 'rotate-180' : ''}`}>
          ▼
        </span>
      </button>
      <div
        className={`overflow-hidden transition-all duration-300 ${isOpen ? 'max-h-96 mt-4' : 'max-h-0'}`}
      >
        <p className="text-text-muted leading-relaxed">{answer}</p>
      </div>
    </div>
  );
};

export default function HomePage() {
  const parallaxOffset = useParallax(0.5);
  const [currentSlide, setCurrentSlide] = useState(0);

  const recommendedCourses = [
    { title: 'Python для начинающих', teacher: 'Иван Иванов', relevance: 95, duration: '10 ч' },
    { title: 'Web разработка', teacher: 'Мария Петрова', relevance: 88, duration: '15 ч' },
    { title: 'Machine Learning', teacher: 'Алексей Смирнов', relevance: 82, duration: '20 ч' },
  ];

  const categories = [
    { name: 'Программирование', courses: 156 },
    { name: 'Дизайн', courses: 89 },
    { name: 'Маркетинг', courses: 67 },
    { name: 'Бизнес', courses: 45 },
  ];

  useEffect(() => {
    const timer = setInterval(() => {
      setCurrentSlide((prev) => (prev + 1) % recommendedCourses.length);
    }, 5000);
    return () => clearInterval(timer);
  }, []);

  return (
    <div className="min-h-screen flex flex-col">
      <Header />

      <main id="main-content" className="flex-grow">
        {/* Hero Section */}
        <section className="relative min-h-screen flex items-center justify-center overflow-hidden py-20 bg-[#0A071B]">
          {/* Premium Animated Background - Enhanced */}
          <div className="absolute inset-0 overflow-hidden">
            {/* Radial gradient base */}
            <div className="absolute inset-0 bg-gradient-radial from-neon-purple/10 via-transparent to-transparent" />

            {/* Large gradient orbs with enhanced glow */}
            <div
              className="absolute w-[800px] h-[800px] rounded-full -top-40 -left-40 animate-float"
              style={{
                background: 'radial-gradient(circle, rgba(177, 60, 255, 0.4) 0%, rgba(177, 60, 255, 0.1) 40%, transparent 70%)',
                transform: `translateY(${parallaxOffset}px)`,
                filter: 'blur(80px)',
              }}
            />
            <div
              className="absolute w-[700px] h-[700px] rounded-full top-1/4 -right-20 animate-float"
              style={{
                background: 'radial-gradient(circle, rgba(255, 77, 255, 0.35) 0%, rgba(255, 77, 255, 0.1) 40%, transparent 70%)',
                animationDelay: '2s',
                transform: `translateY(${-parallaxOffset}px)`,
                filter: 'blur(100px)',
              }}
            />
            <div
              className="absolute w-[650px] h-[650px] rounded-full bottom-0 left-1/4 animate-float"
              style={{
                background: 'radial-gradient(circle, rgba(77, 189, 255, 0.3) 0%, rgba(77, 189, 255, 0.1) 40%, transparent 70%)',
                animationDelay: '4s',
                filter: 'blur(90px)',
              }}
            />
            <div
              className="absolute w-[600px] h-[600px] rounded-full bottom-1/3 right-1/4 animate-float"
              style={{
                background: 'radial-gradient(circle, rgba(255, 46, 136, 0.25) 0%, rgba(255, 46, 136, 0.08) 40%, transparent 70%)',
                animationDelay: '6s',
                filter: 'blur(95px)',
              }}
            />
            <div
              className="absolute w-[500px] h-[500px] rounded-full top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 animate-float"
              style={{
                background: 'radial-gradient(circle, rgba(106, 92, 255, 0.2) 0%, rgba(106, 92, 255, 0.05) 50%, transparent 70%)',
                animationDelay: '3s',
                filter: 'blur(100px)',
              }}
            />

            {/* Animated grid pattern overlay */}
            <div className="absolute inset-0 opacity-[0.02]">
              <div className="absolute inset-0" style={{
                backgroundImage: 'linear-gradient(rgba(177, 60, 255, 0.5) 1px, transparent 1px), linear-gradient(90deg, rgba(177, 60, 255, 0.5) 1px, transparent 1px)',
                backgroundSize: '60px 60px',
              }} />
            </div>
          </div>

          <div className="container mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
            {/* Main Hero Content */}
            <div className="max-w-7xl mx-auto">
              <div className="grid lg:grid-cols-2 gap-12 lg:gap-16 items-center">
                {/* Left: Text Content */}
                <div className="animate-slide-up text-center lg:text-left space-y-8">
                  {/* Badge */}
                  <div className="inline-flex items-center gap-2 px-4 py-2 bg-neon-blue/10 border border-neon-blue/30 rounded-full">
                    <span className="w-2 h-2 bg-neon-cyan rounded-full animate-pulse" style={{ boxShadow: '0 0 10px #4DBDFF' }} />
                    <span className="text-sm font-semibold text-white/90">AI-Powered Learning v2.0</span>
                  </div>

                  {/* Headline - User's Design */}
                  <h1 className="text-5xl sm:text-6xl lg:text-7xl font-black leading-[1.1] tracking-tight">
                    Учитесь в любом месте с <br className="hidden sm:block" />
                    <span className="text-gradient-primary">ONTHEGO</span>
                  </h1>

                  {/* Subheadline */}
                  <p className="text-lg lg:text-xl text-white/70 leading-relaxed max-w-2xl">
                    Образовательная экосистема будущего. Персональные курсы, адаптирующиеся под ваш нейропрофиль в реальном времени.
                  </p>

                  {/* CTA Buttons - User's Style */}
                  <div className="flex flex-col sm:flex-row gap-4 justify-center lg:justify-start">
                    <Link href="/courses">
                      <Button
                        size="lg"
                        variant="primary"
                        style={{
                          borderRadius: '100px',
                          padding: '16px 40px',
                          fontSize: '1.1rem',
                        }}
                      >
                        🚀 Начать обучение
                      </Button>
                    </Link>
                    <Link href="/register">
                      <Button
                        size="lg"
                        variant="outline"
                        style={{
                          borderRadius: '100px',
                          padding: '16px 40px',
                          fontSize: '1.1rem',
                          border: '1px solid rgba(255,255,255,0.2)',
                        }}
                      >
                        ✨ Регистрация
                      </Button>
                    </Link>
                  </div>

                  {/* Stats - User's Design */}
                  <div className="flex gap-10 border-t border-white/10 pt-8 justify-center lg:justify-start">
                    <div>
                      <h3 className="text-4xl font-bold text-white">15k+</h3>
                      <p className="text-white/50 text-sm mt-1">Активных студентов</p>
                    </div>
                    <div>
                      <h3 className="text-4xl font-bold text-neon-cyan">94%</h3>
                      <p className="text-white/50 text-sm mt-1">Трудоустройство</p>
                    </div>
                  </div>
                </div>

                {/* Right: Visual Element - SIMPLIFIED */}
                <div className="hidden lg:flex items-center justify-center animate-fade-in">
                  <div className="relative w-full max-w-md aspect-square">
                    {/* Glowing orb */}
                    <div className="absolute inset-0 flex items-center justify-center">
                      <div className="w-64 h-64 bg-neon-purple/20 rounded-full blur-3xl animate-glow-pulse" />
                    </div>

                    {/* Logo card - SIMPLIFIED */}
                    <div className="relative bg-dark-card/60 backdrop-blur-sm rounded-3xl p-12 border border-neon-purple/20 shadow-2xl">
                      <div className="text-center space-y-6">
                        <div className="text-5xl font-black gradient-text">
                          ONTHEGO
                        </div>
                        <div className="text-sm text-text-muted tracking-widest">
                          AI EDUCATION PLATFORM
                        </div>

                        {/* Status indicators */}
                        <div className="flex justify-center gap-2 pt-4">
                          <div className="w-2 h-2 rounded-full bg-neon-purple animate-pulse" />
                          <div className="w-2 h-2 rounded-full bg-neon-cyan animate-pulse" style={{ animationDelay: '0.2s' }} />
                          <div className="w-2 h-2 rounded-full bg-neon-pink animate-pulse" style={{ animationDelay: '0.4s' }} />
                        </div>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
            </div>

          </div>
        </section>

        {/* Stats Section - Moved from Hero */}
        <LazySection
          fallback={
            <section className="py-16 lg:py-24 bg-dark-primary/50">
              <div className="container mx-auto px-4 sm:px-6 lg:px-8">
                <div className="grid lg:grid-cols-2 gap-8 max-w-7xl mx-auto">
                  <ChartSkeleton />
                  <div className="space-y-8">
                    <ChartSkeleton />
                    <ChartSkeleton />
                  </div>
                </div>
              </div>
            </section>
          }
        >
          <section className="py-16 lg:py-24 bg-dark-primary/50">
            <div className="container mx-auto px-4 sm:px-6 lg:px-8">
              <div className="max-w-7xl mx-auto">
                {/* Section Header */}
                <div className="text-center mb-16">
                  <h2 className="text-3xl lg:text-4xl font-bold text-text-primary mb-4">
                    Платформа в цифрах
                  </h2>
                  <p className="text-lg text-text-secondary max-w-2xl mx-auto">
                    Присоединяйтесь к растущему сообществу профессионалов
                  </p>
                </div>

                {/* Charts Grid */}
                <div className="grid lg:grid-cols-2 gap-8">
                  {/* Large Chart */}
                  <div className="flex items-center justify-center">
                    <SemiDonutChart
                      percentage={78}
                      title="Прогресс обучения"
                      subtitle="78% курсов завершено"
                    />
                  </div>

                  {/* Two Smaller Charts */}
                  <div className="space-y-8">
                    <VerticalBarChart title="Активность студентов" />
                    <SmoothLineChart title="Рост навыков" />
                  </div>
                </div>
              </div>
            </div>
          </section>
        </LazySection>

        {/* Features Section - SIMPLIFIED */}
        <section className="py-16 lg:py-24">
          <div className="container mx-auto px-4 sm:px-6 lg:px-8">
            <div className="max-w-7xl mx-auto">
              {/* Header */}
              <div className="text-center mb-16">
                <h2 className="text-3xl lg:text-5xl font-black text-text-primary mb-4">
                  Почему ONTHEGO?
                </h2>
                <p className="text-lg text-text-secondary max-w-2xl mx-auto">
                  Платформа нового поколения для эффективного обучения
                </p>
              </div>

              {/* Features Grid - CONSISTENT SPACING */}
              <div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-6">
                {[
                  {
                    title: 'Качественные курсы',
                    desc: 'Профессиональные курсы от экспертов индустрии',
                    gradient: 'from-neon-purple to-neon-pink',
                    icon: '📚'
                  },
                  {
                    title: 'AI-персонализация',
                    desc: 'Умные рекомендации под ваш стиль обучения',
                    gradient: 'from-neon-cyan to-neon-blue',
                    icon: '🤖'
                  },
                  {
                    title: 'Учитесь везде',
                    desc: 'Доступ с любого устройства в любое время',
                    gradient: 'from-neon-pink to-neon-magenta',
                    icon: '🌍'
                  },
                ].map((item) => (
                  <div
                    key={item.title}
                    className="card-glow p-6 rounded-2xl text-center group hover:scale-105 transition-all duration-300"
                  >
                    {/* Icon */}
                    <div className={`w-16 h-16 bg-gradient-to-br ${item.gradient} rounded-2xl flex items-center justify-center mx-auto mb-4 group-hover:scale-110 transition-transform`}>
                      <span className="text-3xl">{item.icon}</span>
                    </div>

                    {/* Content */}
                    <h3 className="text-xl font-bold mb-3 text-text-primary">
                      {item.title}
                    </h3>
                    <p className="text-text-muted leading-relaxed">{item.desc}</p>
                  </div>
                ))}
              </div>
            </div>
          </div>
        </section>

        {/* Categories - MOVED UP */}
        <section className="py-16 lg:py-24 bg-gradient-to-b from-dark-primary/50 to-transparent">
          <div className="container mx-auto px-4 sm:px-6 lg:px-8">
            <div className="max-w-7xl mx-auto">
              <div className="text-center mb-12">
                <h2 className="text-3xl lg:text-4xl font-black text-text-primary mb-4">
                  Популярные категории
                </h2>
                <p className="text-lg text-text-secondary">
                  Выберите направление и начните обучение
                </p>
              </div>

              <div className="grid grid-cols-2 md:grid-cols-4 gap-6">
                {categories.map((cat) => (
                  <Link
                    key={cat.name}
                    href="/categories"
                    className="bg-dark-card/40 backdrop-blur-glass p-6 lg:p-8 rounded-2xl text-center border border-neon-purple/10 hover:border-neon-purple/30 hover:scale-105 transition-all duration-300 group"
                  >
                    <div className="w-16 h-16 lg:w-20 lg:h-20 bg-gradient-to-br from-neon-purple via-neon-pink to-neon-cyan rounded-2xl mx-auto mb-4 lg:mb-6 group-hover:shadow-lg group-hover:shadow-neon-purple/50 transition-all duration-300 group-hover:rotate-12" />
                    <h3 className="font-bold text-base lg:text-lg text-text-primary mb-2 group-hover:text-neon-cyan transition-colors">
                      {cat.name}
                    </h3>
                    <p className="text-text-muted text-sm">
                      <span className="font-semibold text-neon-purple">{cat.courses}</span> курсов
                    </p>
                  </Link>
                ))}
              </div>

              <div className="text-center mt-8 lg:mt-12">
                <Link href="/categories">
                  <Button variant="outline" size="lg">
                    Все категории
                  </Button>
                </Link>
              </div>
            </div>
          </div>
        </section>

        {/* AI Recommendations Slider */}
        <section className="py-16 lg:py-24 bg-dark-primary/50">
          <div className="container mx-auto px-4">
            <div className="flex justify-between items-center mb-8">
              <h2 className="text-4xl font-bold text-text-primary">Мини-рекомендации</h2>
              <Link href="/courses" className="text-neon-cyan hover:text-neon-pink transition">
                Все рекомендации →
              </Link>
            </div>

            <div className="relative">
              <div className="overflow-hidden">
                <div
                  className="flex transition-transform duration-500"
                  style={{ transform: `translateX(-${currentSlide * 100}%)` }}
                >
                  {recommendedCourses.map((course, i) => (
                    <div key={i} className="w-full flex-shrink-0 px-2">
                      <div className="card-glow rounded-xl p-6 hover:scale-105 transition-transform">
                        <h3 className="text-2xl font-bold text-text-primary mb-2">{course.title}</h3>
                        <p className="text-text-muted mb-4">Преподаватель: {course.teacher}</p>
                        <div className="flex items-center justify-between mb-4">
                          <span className="text-sm text-text-muted">{course.duration}</span>
                          <div className="flex items-center">
                            <div className="w-24 h-2 bg-dark-card rounded-full overflow-hidden">
                              <div
                                className="h-full bg-gradient-to-r from-neon-purple to-neon-pink"
                                style={{ width: `${course.relevance}%` }}
                              />
                            </div>
                            <span className="ml-2 text-neon-cyan font-semibold">{course.relevance}%</span>
                          </div>
                        </div>
                        <Button variant="primary" fullWidth>
                          Начать курс
                        </Button>
                      </div>
                    </div>
                  ))}
                </div>
              </div>

              {/* Slider Dots */}
              <div className="flex justify-center mt-6 space-x-2">
                {recommendedCourses.map((_, i) => (
                  <button
                    key={i}
                    onClick={() => setCurrentSlide(i)}
                    aria-label={`Слайд ${i + 1}`}
                    className={`w-2 h-2 rounded-full transition-all ${i === currentSlide ? 'w-8 bg-neon-purple' : 'bg-neon-purple/30'}`}
                  />
                ))}
              </div>
            </div>
          </div>
        </section>

        {/* FAQ Section */}
        <LazySection
          fallback={
            <section className="py-16 lg:py-24 bg-dark-primary/30">
              <div className="container mx-auto px-4 sm:px-6 lg:px-8">
                <div className="max-w-3xl mx-auto space-y-4">
                  {[1, 2, 3].map((i) => (
                    <div key={i} className="h-20 bg-dark-card/30 rounded-xl animate-pulse" />
                  ))}
                </div>
              </div>
            </section>
          }
        >
          <section className="py-16 lg:py-24 bg-dark-primary/30">
            <div className="container mx-auto px-4 sm:px-6 lg:px-8">
              <div className="max-w-3xl mx-auto">
                {/* Header */}
                <div className="text-center mb-12">
                  <h2 className="text-3xl lg:text-4xl font-bold text-text-primary mb-4">
                    Частые вопросы
                  </h2>
                  <p className="text-lg text-text-secondary">
                    Ответы на популярные вопросы о платформе
                  </p>
                </div>

                {/* FAQ Items */}
                <div className="space-y-4">
                  <FAQItem
                    question="Как начать обучение?"
                    answer="Зарегистрируйтесь на платформе, выберите интересующий курс и начните обучение прямо сейчас!"
                  />
                  <FAQItem
                    question="Есть ли бесплатные курсы?"
                    answer="Да, многие курсы доступны бесплатно. Платные курсы предлагают расширенный функционал."
                  />
                  <FAQItem
                    question="Можно ли учиться с мобильного?"
                    answer="Конечно! Платформа полностью адаптирована для всех устройств."
                  />
                </div>
              </div>
            </div>
          </section>
        </LazySection>

        {/* CTA Section - SIMPLIFIED & CLEAN */}
        <section className="py-16 lg:py-24 relative overflow-hidden">
          {/* Background Gradient */}
          <div className="absolute inset-0 bg-gradient-to-br from-neon-purple/5 via-transparent to-neon-cyan/5" />

          <div className="container mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
            <div className="max-w-4xl mx-auto">
              {/* Main CTA Card */}
              <div className="card-glow rounded-3xl p-8 md:p-12 text-center space-y-8">
                {/* Headline */}
                <div className="space-y-4">
                  <h2 className="text-4xl lg:text-5xl font-black text-text-primary">
                    Начните учиться сегодня
                  </h2>
                  <p className="text-lg lg:text-xl text-text-secondary max-w-2xl mx-auto">
                    Присоединяйтесь к 50,000+ студентов, которые уже развивают свои навыки
                  </p>
                </div>

                {/* CTA Buttons */}
                <div className="flex flex-col sm:flex-row gap-3 justify-center">
                  <Link href="/register">
                    <Button size="lg" variant="primary" className="shadow-lg shadow-neon-purple/50">
                      Регистрация
                    </Button>
                  </Link>
                  <Link href="/courses">
                    <Button size="lg" variant="outline">
                      Смотреть курсы
                    </Button>
                  </Link>
                </div>

                {/* Trust Bar - MINIMAL */}
                <div className="flex items-center justify-center gap-8 pt-8 border-t border-neon-purple/10 text-sm text-text-muted">
                  <div className="flex items-center gap-2">
                    <span className="text-neon-purple font-semibold">500+</span>
                    <span>курсов</span>
                  </div>
                  <div className="w-px h-4 bg-text-muted/20" />
                  <div className="flex items-center gap-2">
                    <span className="text-neon-cyan font-semibold">95%</span>
                    <span>довольны</span>
                  </div>
                  <div className="w-px h-4 bg-text-muted/20" />
                  <div className="flex items-center gap-1">
                    <span className="text-yellow-400">★</span>
                    <span className="text-neon-pink font-semibold">4.9</span>
                    <span>рейтинг</span>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </section>
      </main>

      <Footer />
    </div>
  );
}
