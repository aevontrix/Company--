# ONTHEGO Frontend

🚀 AI-powered образовательная платформа - Premium Frontend веб-сервис на Next.js 14

## ✨ Дизайн

Современный **неоновый** дизайн с премиум эффектами:
- 🎨 Неоновая цветовая палитра (розовый, фиолетовый, голубой)
- ✨ Анимированные градиенты и эффекты свечения
- 🌙 Темная тема с глубоким фиолетовым фоном
- 💫 Плавные анимации и переходы
- 🔮 Стеклянные эффекты (glassmorphism)
- ⚡ Интерактивные hover-эффекты

### Цветовая палитра

**Основные неоновые цвета:**
- Розовый неон: `#FF4DFF`
- Фиолетовый неон: `#B13CFF`
- Сине-фиолетовый: `#6A5CFF`
- Голубой неон: `#4DBDFF`
- Малиновый неон: `#FF2E88`

**Фоновые цвета:**
- Темный фиолетовый: `#0A071B`
- Черный с фиолетовым: `#05040F`

## 🛠 Технологии

- **Next.js 14** - React фреймворк с App Router
- **TypeScript** - Типизированный JavaScript
- **Tailwind CSS** - Кастомизированные неоновые цвета
- **Zustand** - Управление состоянием
- **Axios** - HTTP клиент для API запросов
- **JWT** - Аутентификация через токены

## 📦 Установка

### Предварительные требования

- Node.js 18+ и npm
- Django backend запущен на `http://localhost:8000`

### Шаги установки

1. **Установите Node.js**
   - Скачайте с [nodejs.org](https://nodejs.org/)
   - Рекомендуется LTS версия (18.x или 20.x)

2. **Установите зависимости**
   ```bash
   cd c:/OntheGo/frontend
   npm install
   ```

3. **Настройте переменные окружения**
   Файл `.env.local` уже создан со следующими настройками:
   ```env
   NEXT_PUBLIC_API_URL=http://localhost:8000/api
   ```

4. **Запустите development сервер**
   ```bash
   npm run dev
   ```

   Приложение будет доступно по адресу: http://localhost:3000

## 📁 Структура проекта

```
frontend/
├── app/                    # Next.js App Router страницы
│   ├── page.tsx           # Главная страница
│   ├── login/             # Страница входа
│   ├── register/          # Страница регистрации
│   ├── courses/           # Страница курсов
│   └── my-learning/       # Личный кабинет обучения
├── components/            # React компоненты
│   ├── layout/           # Компоненты layout (Header, Footer)
│   ├── ui/               # UI компоненты (Button, Input)
│   └── courses/          # Компоненты курсов
├── lib/                  # Библиотеки и утилиты
│   ├── api/             # API клиенты
│   ├── store/           # Zustand stores
│   ├── types/           # TypeScript типы
│   └── utils/           # Утилиты
└── public/              # Статические файлы
```

## ✅ Основные возможности

### Аутентификация
- Регистрация и вход пользователей
- JWT токены (access и refresh)
- Автоматическое обновление токенов
- Защищенные маршруты

### Курсы
- Просмотр всех курсов
- Фильтрация по категориям и сложности
- Поиск курсов
- Детальная информация о курсе
- Запись на курс

### Личный кабинет
- Мои курсы
- Прогресс обучения
- Профиль пользователя

## 🔌 API Integration

Фронтенд интегрирован с Django REST API:
- Базовый URL: `http://localhost:8000/api`
- Автоматическая обработка JWT токенов
- Обработка ошибок и повторные запросы

### Доступные API endpoints

```typescript
// Аутентификация
POST /users/login/
POST /users/register/
POST /users/logout/
GET  /users/profile/

// Курсы
GET  /courses/
GET  /courses/{slug}/
GET  /courses/featured/
GET  /courses/popular/
GET  /courses/categories/

// Обучение
POST /learning/enrollments/
GET  /learning/enrollments/
GET  /learning/lesson-progress/{id}/
```

## 📝 Скрипты

```bash
# Development сервер
npm run dev

# Production build
npm run build

# Запуск production сервера
npm run start

# Линтинг
npm run lint
```

## 📱 Мобильная адаптация

Все компоненты созданы с responsive дизайном:
- Mobile-first подход
- Tailwind CSS breakpoints: `sm`, `md`, `lg`, `xl`
- Адаптивные сетки и layout
- Оптимизация для сенсорных экранов
- Неоновые эффекты оптимизированы для мобильных устройств

## 🎨 Дизайн-система

### Компоненты

**Button** - 5 вариантов с градиентами:
- `primary` - фиолетово-розовый градиент
- `secondary` - фиолетово-синий градиент
- `outline` - обводка с неоновым свечением
- `danger` - сине-розовый градиент
- `ghost` - прозрачная с hover эффектом

**Input** - неоновая обводка с фокус-эффектами
**CourseCard** - карточки с эффектами свечения и анимированной рамкой

### Эффекты

- `glow-pink/purple/blue/cyan` - текстовое свечение
- `card-glow` - свечение карточек с hover
- `gradient-text` - градиентный текст
- `backdrop-blur-glass` - стеклянный эффект
- Анимации: `float`, `glow-pulse`, `slide-up`, `fade-in`

## 🚀 Следующие шаги

1. **Установите Node.js** (если еще не установлен)
2. **Запустите `npm install`** в директории frontend
3. **Убедитесь, что Django backend запущен** на порту 8000
4. **Запустите `npm run dev`**
5. **Откройте http://localhost:3000** в браузере

## 🔧 Troubleshooting

### Проблема: CORS ошибки
**Решение:** Убедитесь, что в Django backend настроены CORS:
```python
CORS_ALLOWED_ORIGINS = [
    'http://localhost:3000',
    'http://127.0.0.1:3000',
]
```

### Проблема: API connection refused
**Решение:** Проверьте, что Django сервер запущен:
```bash
cd c:/OntheGo/backend
venv\Scripts\activate
python manage.py runserver
```

### Проблема: npm install fails
**Решение:** Попробуйте:
```bash
npm cache clean --force
npm install
```

## 📚 Документация

- [NEON_DESIGN.md](../NEON_DESIGN.md) - Подробное описание неонового дизайна
- [FRONTEND_SETUP.md](../FRONTEND_SETUP.md) - Инструкция по запуску

## 🎯 Особенности дизайна Senior-уровня

- ✅ Анимированные градиентные фоны
- ✅ Эффекты свечения с box-shadow
- ✅ Glassmorphism эффекты
- ✅ Smooth transitions на всех элементах
- ✅ Hover эффекты с transform
- ✅ Sticky header с backdrop blur
- ✅ Кастомный scrollbar
- ✅ Responsive анимации
- ✅ Gradient текст
- ✅ Loading состояния
- ✅ Empty states с эмоджи
- ✅ Accessibility (focus states)

## 💡 Поддержка

Для вопросов и проблем создайте issue в репозитории проекта.

---

**Создано с 💜 для ONTHEGO**
